# SDD ledger — plan: docs/superpowers/plans/2026-08-29-live-scores.md
- Task 3 — bespoke team picker (Henry overruled the shared SearchableList; plan updated in the same commit). 8 tests added (suite 2591 -> 2599). Guard proven red: removing setQuery("") on close -> "THE GUARD: the query resets when the popover closes". Drove the real browser: search by abbreviation and full name, all four leagues grouped, select persists the whole favourite with cached logo + colour, row survives reload, remove empties the store.
- Task 2 — integration, schedule, registration chain. 14 tests added (suite 2577 -> 2591). Guards proven red: NaN poll delay (plan's suggested mutation was inert — `NaN > now` is already false; dropping the whole filter is what yields "delayMs was NaN"), half-registered integration ("No automation entries for: scores", "31 stores declared but 30 registered"), broadcast-on-change (`fetchedAt` sensitivity re-added -> a 25s poll becomes a 25s SSE frame). Plan bug fixed: StatusIntegration.emitIfChanged is a SHALLOW compare and would have broadcast every poll. Drove the real server: favourites persist with cached logo, teams endpoint returns live ESPN data, hello burst carries scores:status, enabling the integration polls and returns both doubleheader games.
- Task 1 — ESPN client + pure folds. 17 tests added (suite 2560 -> 2577). Guards proven red: doubleheader diff key (team-only key reports BOS from:0 to:6 against the other half), null score coercion (`?? 0` -> "0 !== null"), possession-vs-possessionText ("SJSU 28" is neither 23 nor 30), possession/downDistance independence (football arm bailing on absent possession drops "1st & 10").
