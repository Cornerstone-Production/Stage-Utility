One fix to 1.17.0. The Home Assistant fragment from Copy YAML now writes its
switches under the `template:` key, the current template format. The legacy
`platform: template` form 1.17.0 generated is refused by current Home Assistant
with a repair notice. Copy the fragment again and replace the one you saved; each
switch's entity id now follows its name.
