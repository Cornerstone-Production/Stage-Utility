# Dependencies
node_modules/

# Build output
build/
.vite/

# Env / secrets
.env
.env.local
*.key

# macOS
.DS_Store

# Editor
.vscode/
*.swp

# Logs
*.log
npm-debug.log*

# Glaze memory/internal
.mic-display-memory/
.mcp.json

# Claude Code local config
.claude/

# Local SenSource API diagnostic captures (contain org names/addresses/MACs — never commit)
sensource-capture*.json
sensource-probe*.json

# Local tooling scratch (Playwright MCP screenshots/logs, impeccable design plugin)
.playwright-mcp/
.impeccable/

# Internal working documents. These are notes to ourselves — agent execution
# plans with task checkboxes, proposals with a "decisions for you" section, and
# references to local tooling paths that exist on no other machine. Published
# design docs belong in docs/ proper, written for a stranger.
docs/superpowers/
docs/design-overhaul/
DESIGN_TOKENS_PROPOSAL.md
